#program which accepts the radius of a circle from the user and compute the area.

from math import pi
r = float(input("input your number"))

print("The are of the circle with radius" + str() + " ls:" + str(pi * r**2))
